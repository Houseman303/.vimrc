# remap escape key to 'jk' (macbook pro touchbar escape key, also keep hands on home row)
inoremap jk <ESC>

# make tabs 4 spaces (python primary programming language)
set tabstop=4
set shiftwidth=4
set expandtab
